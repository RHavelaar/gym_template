import { BrandPreviewBodyLock } from "@/components/admin/settings/brand-preview-body-lock";

export default function BrandPreviewEmbedLayout({ children }: { children: React.ReactNode }) {
  return <BrandPreviewBodyLock>{children}</BrandPreviewBodyLock>;
}
